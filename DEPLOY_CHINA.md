# 中国大陆静态部署说明

## 推荐方式：腾讯云 CloudBase 静态网站托管

1. 在项目根目录运行 `npm install` 和 `npm run build`。
2. 在 CloudBase 控制台启用静态网站托管。
3. 上传 **`dist` 目录里面的全部内容**，不要在托管根目录外再套一层 `dist` 文件夹。
4. 将默认首页设置为 `index.html`。
5. 发布后确认 `audio/bgm.mp3` 可访问，并实际点击“轻触开启”测试音乐。

## 发布检查

发布后请分别使用以下环境检查完整流程：

- 中国大陆手机流量；
- 桌面普通浏览器；
- 微信内置浏览器。

重点检查音乐首次点击播放、触摸拖动花束和蛋糕、烛光许愿、重新播放及竖屏安全区域。GitHub Pages 可作为备用链接，但不应作为“中国大陆稳定访问”的唯一成品链接。

## GitHub Pages 备用发布

仓库根目录的 `docs/` 与已验证的 `dist/` 构建产物保持一致，资源均使用相对路径。GitHub Pages 的发布来源可设置为当前分支的 `/docs` 目录；该方式仅作为备用访问渠道。
