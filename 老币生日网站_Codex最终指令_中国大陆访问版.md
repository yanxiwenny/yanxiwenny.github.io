# Codex 最终任务：老币生日惊喜网站（中国大陆可访问版）

## 一、任务目标

请直接在当前 GitHub 仓库 `yanxiwenny/yanxiwenny.github.io` 中完成一个精致、可交互、适配手机和电脑的单页生日惊喜网站。

祝福对象与核心文字：

- 昵称：`老币`
- 主标题：`老币，生日快乐`
- 背景音乐最终文件：`bgm.mp3`

最终效果不是普通网页卡片，也不是简单粒子动画。它应当像一段可以亲手打开的生日短片：用户点击开启后，先看到梦幻粒子汇聚成立体栀子花束，随后花束化作星尘，转场到精致的深蓝星空蛋糕；背景中一直有低透明度代码缓慢向上放映，如电影片尾字幕，但不能干扰花、蛋糕和祝福文字。

请直接创建、修改、构建、检查并提交完整文件。不要只返回代码片段、教程或设计建议。

---

## 二、开始前必须执行

1. 先检查仓库现有文件和分支，不要假设仓库为空。
2. 如果存在上一版网站，保留有价值的交互和素材，但必须重做过于简陋的花束、蛋糕、文字排版和背景。
3. 检查仓库根目录是否有 `bgm.mp3`：
   - 如果有，将它移动或复制到 `public/audio/bgm.mp3`，并确保构建后出现在 `dist/audio/bgm.mp3`。
   - 如果没有，不要中止任务；先创建 `public/audio/` 和说明文件，网站仍须完整运行。音乐按钮应显示“音乐待加入”或静音状态，不能持续报错。
4. 使用 Vite + Three.js 实现，依赖必须安装并打包进最终产物；网站运行时不得依赖境外 CDN。
5. 如果网络权限导致依赖无法安装，明确报告具体阻碍，不要改成引用 unpkg、jsDelivr、Google Fonts、GitHub Raw 或其他境外运行时资源。

---

## 三、技术与发布硬性要求

### 3.1 技术栈

- Vite
- Three.js
- 原生 HTML、CSS、JavaScript
- 如使用 Three.js 后期效果，只能使用随 `three` 包一起安装的本地模块，例如 `EffectComposer`、`RenderPass`、`UnrealBloomPass`。
- 不需要后端、数据库、登录或表单。

### 3.2 中国大陆访问兼容

最终网站将主要发布到中国大陆可访问的静态托管服务，因此必须满足：

- 所有 JavaScript、样式、字体、图片、纹理、音频和模型均为本地文件或构建进产物。
- 禁止在网页运行时访问 Google Fonts、unpkg、jsDelivr、cdnjs、GitHub Raw、Vercel、Cloudflare 等外部资源。
- 中文字体使用系统字体栈，例如：`STKaiti, KaiTi, Microsoft YaHei, PingFang SC, Noto Serif CJK SC, serif`，不得远程加载字体。
- `vite.config.js` 设置 `base: './'`。
- 所有资源使用相对路径，不得把 `/audio/...` 等根路径写死。
- 执行 `npm run build` 后，`dist` 目录必须可以作为一个完整静态网站直接上传。
- 根入口必须是 `dist/index.html`。
- 除音乐文件外，打开网站后不应再请求任何第三方域名。

### 3.3 需要生成的文件

至少包括：

```text
index.html
package.json
package-lock.json
vite.config.js
src/
  main.js
  styles.css
  scenes/
  utils/
public/
  audio/
    bgm.mp3 或 MUSIC_README.txt
README.md
DEPLOY_CHINA.md
dist/
```

允许根据实现调整源码结构，但不得缺少可直接上传的 `dist` 成品目录。

---

## 四、整体美术方向

视觉关键词：

`浅暮色星空、蓝紫雾光、奶油白栀子花、深海蓝蛋糕、香槟金、玻璃感、电影片尾代码、克制而梦幻`

### 4.1 色彩

页面不能像上一版那样大面积接近黑色。使用偏浅、柔和但仍有空间感的暮色渐变，例如：

- 页面远景：`#DCE5F4`、`#BFCBE5`、`#A7B8DC`、`#8D9FC8`
- 深层雾影：`#556A98`
- 蛋糕主体：`#102A59`、`#173B78`、`#24519A`
- 栀子花：`#FFFDF4`、`#F7F0DC`
- 叶片：`#3F765F`、`#78A58B`
- 金色装饰：`#E7C777`、`#F5DFA0`
- 文字：偏暖白和深蓝灰，不使用刺眼纯白。

必须保证主体和文字有足够对比度，同时避免纯黑背景、荧光绿代码雨、廉价霓虹色和过饱和蓝色。

### 4.2 灯光与质感

- 使用柔和环境光、主光、暖色轮廓光和冷色补光。
- 花瓣、糖霜、金边、珍珠和玻璃托盘要有不同材质参数，不能全部像塑料。
- 使用柔和阴影、轻微景深感、克制的 Bloom 和空气雾。
- Bloom 只能突出烛火、金色星屑和光环，不能让整个页面泛白。

---

## 五、背景代码：电影片尾放映效果

这是本次新增的核心要求，必须实现。

1. 背景代码从欢迎页开始一直存在，位于 3D 场景与文字之后。
2. 不做《黑客帝国》式绿色代码雨。应像电影片尾字幕或开发过程被缓慢放映：
   - 两到三列 JavaScript/Three.js 风格代码从页面下方向上缓慢滚动；
   - 行距舒展，速度稳定，一次循环约 45–70 秒；
   - 不跳动、不快速闪烁、不打字机式频闪；
   - 上下使用渐隐遮罩，代码进入和离开画面时自然淡化。
3. 代码颜色使用浅蓝灰、雾紫或暖白，整体 `opacity` 建议为 `0.10–0.18`；移动端降至 `0.06–0.12`。
4. 代码要能“稍微看清”，但绝不能盖过花束、蛋糕、按钮和祝福文字。
5. 可以显示与网站有关的优雅代码片段，例如：

```js
const wish = createWish('老币');
gardenias.bloom({ with: 'starlight' });
cake.decorate(['moon', 'pearls', 'golden stars']);
while (today.isSpecial) happiness += 1;
```

6. 可穿插少量类似片尾字幕的注释：

```js
// Made with starlight for 老币
// One more orbit around the sun
// May every ordinary day softly glow
```

7. 代码仅作为视觉背景，使用 `aria-hidden="true"`，不可被选中，不阻挡触摸或鼠标操作。
8. 对 `prefers-reduced-motion: reduce` 用户停止滚动，仅显示静态淡化代码。

---

## 六、完整叙事与交互流程

### 场景 0：等待开启

- 首屏保持简洁，浅暮色雾光与微弱星尘已经存在，背景代码缓慢上移。
- 中央文字不要直接堆砌“生日快乐”，先显示一句：`有一束花，替星光先来见你。`
- 主按钮：`轻触开启`
- 次要小字：`建议打开声音`
- 按钮使用细腻玻璃质感和柔和光边，避免巨大圆角卡片和模板化布局。
- 必须由用户点击后再播放音乐，以兼容手机、微信浏览器和 iOS 自动播放限制。

### 场景 1：粒子汇聚成立体栀子花束

- 点击后音乐以约 1.5 秒淡入，音量约 `0.38–0.48`。
- 数百至数千个暖白、淡金、浅蓝粒子从四周缓慢汇聚。
- 粒子不是简单聚成一朵平面花，而是逐步显现一束完整、立体、有前后层次的栀子花束。
- 花束完整展示约 6–9 秒，并缓慢自动旋转。
- 用户可通过鼠标拖动或手指横向拖动查看花束侧面；松手后恢复缓慢自动旋转。
- 花束下方可出现一句很轻的文字：`愿这束栀子，把今天变得柔软一点。`
- 设置一个不突兀的小按钮：`收下这束花`，也允许一定时间后自动进入下一幕。

### 场景 2：星尘转场

- 花束边缘先化为金白色微粒，再从中心向外轻轻散开。
- 镜头不要硬切、黑屏或突然缩放。
- 粒子沿螺旋轨迹汇入中央，形成围绕蛋糕的旋转光环。
- 转场持续约 1.8–2.8 秒。

### 场景 3：精致星空蛋糕

- 蛋糕从雾光中缓慢升起并完成一次约 25–35 度的展示旋转。
- 光环围绕蛋糕不同高度旋转，轨道略有倾斜，包含星屑、微光球和小型行星元素。
- 蛋糕可鼠标/触摸拖动旋转；默认以很慢速度自动转动。
- 祝福文字分两步出现：
  1. 较小昵称：`老币`
  2. 主标题：`生日快乐`
- 两行文字不能使用普通网页大黑体。使用有呼吸感的中文字体栈、细金色描边或柔和辉光，字距和行距经过设计。
- 主标题出现后，少量金色星点向上绽放，不能是满屏廉价烟花。

### 场景 4：许愿互动

- 烛火可点击或轻触。
- 提示文字：`轻触烛光，许个愿吧`
- 点击后火焰柔和熄灭，短暂出现一圈金色微光和少量星屑。
- 最终文字：`愿新一岁，自由、明亮，永远有热爱。`
- 提供两个克制的小控件：`重新播放`、`音乐开/关`。
- 不要求麦克风吹蜡烛，避免权限弹窗和兼容性问题。

---

## 七、栀子花束的 3D 精度要求

上一版花过于简陋，本版必须把花束作为主角之一。

### 7.1 花朵结构

- 整束建议包含 5–7 朵大小和开放程度不同的栀子花，其中至少 3 朵完全开放、1–2 朵半开、1 个花苞。
- 每朵花至少包含 3 层错位花瓣；外层宽大舒展，中层卷曲，内层密集。
- 不允许用若干白色圆片或规则圆环冒充花瓣。
- 花瓣使用曲面几何，有可见的弯曲、厚薄变化和不完全对称。
- 花瓣根部偏暖奶油色，边缘偏冷白，中心带极淡金色。
- 材质建议使用 `MeshPhysicalMaterial` 或等效方案，带轻微透光、粗糙度和柔和高光。

### 7.2 花束结构

- 花朵之间必须有前后遮挡、不同朝向和高度，形成真实花束轮廓。
- 加入细茎、深浅不同的叶片、少量花苞和半透明包装纱/纸。
- 叶片应有主脉感、弯曲和双面材质，避免平直绿色椭圆片。
- 包装不宜抢眼，可使用淡蓝灰半透明材质与一条细香槟金丝带。
- 整束花的旋转中心和构图必须稳定，不能像零散模型悬浮。

### 7.3 粒子与花束关系

- 粒子应沿着预设目标点汇聚到花瓣、叶片和轮廓附近。
- 不要求每个粒子最终变成网格顶点，但视觉上必须让观众感到“星尘生成花束”。
- 花束成形后，残余粒子在边缘缓慢漂浮，不能全部消失。

---

## 八、星空蛋糕的 3D 精度要求

上一版蛋糕过于简单，本版必须有明确装饰层次。

### 8.1 蛋糕主体

- 使用两层或高低错落的单体精品蛋糕结构，不做一个简单蓝色圆柱。
- 蛋糕表面使用深蓝渐变、细腻粗糙度和轻微星云纹理。
- 顶部糖霜应有自然起伏和少量下垂边缘，不是完全平整圆片。
- 底部有玻璃或磨砂银蓝色托盘，并能接住柔和阴影。

### 8.2 至少包含以下装饰

- 香槟金细边或不规则金箔；
- 大小不同的金色星星与银蓝珍珠；
- 一枚小月牙或月球装饰；
- 2–3 朵缩小版栀子花作为顶部/侧面装饰；
- 少量半透明糖片或水晶碎片；
- 一根精致蜡烛和具有内外焰层次的动态火焰；
- 围绕蛋糕的两到三条倾斜星轨光环。

所有装饰应围绕统一构图摆放，避免像随机贴纸。

### 8.3 烛火

- 火焰由暖黄内焰、橙金外焰和微弱光晕组成。
- 使用轻微摆动和大小变化模拟呼吸感，不快速抖动。
- 火焰作为点光源轻微照亮顶部糖霜与邻近装饰。
- 熄灭后灯光同步柔和衰减，不得瞬间消失。

---

## 九、音乐处理

1. 音频路径统一为相对路径 `./audio/bgm.mp3`，构建后文件位于 `dist/audio/bgm.mp3`。
2. 音乐必须在用户点击“轻触开启”后调用 `play()`，不得尝试页面加载即自动播放。
3. 音乐淡入约 1.5 秒；默认音量不超过 `0.48`。
4. 可以循环播放，但循环衔接应尽量自然。
5. 提供可见但不抢眼的音乐开关，并正确设置 `aria-label`。
6. 如果文件缺失、加载失败或浏览器阻止播放：
   - 页面动画必须继续；
   - 不弹出错误框；
   - 音乐按钮显示可理解的状态；
   - 控制台只允许一条清晰警告，不能反复报错。

---

## 十、响应式、性能和兼容性

- 优先保证常见手机竖屏 `360–430 px` 宽度下效果完整，因为收礼人很可能通过微信打开。
- 同时适配平板和桌面浏览器。
- 手机端背景代码减少为 1–2 列并进一步降低透明度。
- 3D 画布不得导致页面横向滚动。
- 根据设备能力动态调整：
  - `renderer.setPixelRatio(Math.min(devicePixelRatio, 1.5 或 2))`；
  - 手机减少粒子数量、阴影分辨率和后期效果采样；
  - 页面失去焦点时暂停或降低动画负载；
  - 场景切换后释放不再使用的几何体、材质和纹理。
- 提供 WebGL 不可用时的优雅降级页面，至少显示静态星光背景、祝福文字和音乐按钮。
- 支持触摸拖动，避免只有鼠标事件。
- 遵守 `prefers-reduced-motion`，降低旋转、粒子和代码滚动。
- 避免大面积闪烁、频闪和过快镜头运动。

---

## 十一、文字与界面细节

- 页面语言使用自然中文，不要出现开发术语、模板占位符或 AI 式说明。
- 不使用俗气文案、爱心堆叠、Emoji 大量堆砌或“尊贵用户”等模板文字。
- 主要文案仅使用以下内容，除非为了按钮状态增加极短提示：
  - `有一束花，替星光先来见你。`
  - `轻触开启`
  - `建议打开声音`
  - `愿这束栀子，把今天变得柔软一点。`
  - `收下这束花`
  - `老币`
  - `生日快乐`
  - `轻触烛光，许个愿吧`
  - `愿新一岁，自由、明亮，永远有热爱。`
  - `重新播放`
  - `音乐开/关`
- 按钮、文字与 3D 主体之间保持安全距离，手机端不遮挡蛋糕或花束。
- 不使用粗重阴影、大面积磨砂卡片、默认 Bootstrap 风格或低质量渐变按钮。

---

## 十二、构建、检查与交付

完成代码后必须执行：

```bash
npm install
npm run build
```

并完成以下检查：

1. 构建无错误，`dist/index.html` 存在。
2. 所有资源路径均为相对路径。
3. 页面运行时不请求任何第三方域名。
4. 未提供 `bgm.mp3` 时页面仍可完整播放动画。
5. 提供 `bgm.mp3` 后，点击“轻触开启”可以播放音乐。
6. 桌面端能够拖动旋转花束和蛋糕。
7. 手机触摸旋转、按钮点击和页面布局正常。
8. 花束和蛋糕有真正立体结构、材质、灯光与装饰，不是简单几何体拼接。
9. 背景代码确实缓慢向上放映，稍微可读但不抢主体。
10. 主流程可以完整重复播放，不会出现叠加的定时器、重复音乐或内存持续增长。

如果运行环境支持本地浏览器预览，请至少检查：

- 桌面尺寸约 `1440 × 900`；
- 手机尺寸约 `390 × 844`；
- 欢迎页、花束完整成形、蛋糕最终页三个关键画面。

### README.md 必须写清楚

- 本地运行方法；
- `bgm.mp3` 的正确放置位置；
- 如何重新构建；
- `dist` 是国内静态托管需要上传的最终目录；
- 网站不依赖境外 CDN。

### DEPLOY_CHINA.md 必须写清楚

- 推荐将 `dist` 目录内容上传到腾讯云 CloudBase 静态网站托管；
- 上传的是 `dist` 里面的内容，不是再套一层 `dist` 文件夹；
- 默认首页为 `index.html`；
- 发布后必须分别用中国大陆手机流量、普通浏览器和微信内置浏览器测试；
- GitHub Pages 仅可作为备用链接，不作为“中国大陆稳定访问”的唯一成品链接。

### 任务结束时

1. 提交全部源代码和配置。
2. 如果允许，创建 Pull Request。
3. 汇报时列出：
   - 完成了哪些视觉和交互；
   - 音乐是否已找到并打包；
   - `npm run build` 是否通过；
   - `dist` 的位置；
   - 是否仍有需要用户手动完成的步骤。
4. 不要声称 GitHub Pages 在中国大陆“保证可访问”。

---

## 十三、禁止事项

- 禁止只生成一个蓝色圆柱体当蛋糕。
- 禁止只用白色圆点或平面花瓣当栀子花束。
- 禁止用外部图片代替 3D 花束和 3D 蛋糕。
- 禁止运行时依赖境外 CDN、在线字体或远程 Three.js。
- 禁止满屏黑色、荧光绿色代码雨、刺眼霓虹、快速闪烁。
- 禁止让背景代码盖住主视觉或文字。
- 禁止自动弹出音频权限、麦克风权限或全屏权限。
- 禁止只留下源代码而没有构建成功的 `dist`。
- 禁止仅给出操作说明而不实际修改仓库。

请以完成可交付作品为目标，直接开始检查仓库、实现、构建、验证并提交。
